# ─────────────────────────────────────────────────────────────────────────────
#  CEDAE Inovação — template de variáveis de ambiente
#
#  Como usar:
#    cp .env.example .env        (nunca commite o arquivo .env)
#    cp .env.example backend/.env
#
#  Credenciais Oracle: solicitar ao DBA (bl202.cedae.corp)
#  Autenticação: Kerberos/IIS → header x-remote-user (sem senha no sistema)
# ─────────────────────────────────────────────────────────────────────────────

# ── Conexão Oracle ────────────────────────────────────────────────────────────
ORACLE_HOST=<host-oracle-aqui>
ORACLE_PORT=1521
ORACLE_SERVICE=<service-name-aqui>
ORACLE_USER=<usuario-oracle-aqui>
ORACLE_PASSWORD=<senha-oracle-aqui>

# ── Servidor ──────────────────────────────────────────────────────────────────
PORT=8095

# ── Desenvolvimento local ─────────────────────────────────────────────────────
# Simula o header x-remote-user injetado pelo IIS/Kerberos em produção.
# Defina com o seu login de domínio. O sistema consultará ADMIN_USERS.
# Remover (ou deixar vazio) para testar como usuário comum (sem painel admin).
DEV_REMOTE_USER=gsalviete

# ── Rate limiting (requisições por IP) ───────────────────────────────────────
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100
